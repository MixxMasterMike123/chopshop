# SnapWear launch — ToDo (single source of truth)

Status legend: ☐ open · ◐ in progress · ☑ done · ⏸ waiting on someone
Owner: **K** = Kent · **M** = Mikael · **C** = Claude (Mikael runs) · **N** = Natalia/SnapWear

Rule: nothing goes live with a SnapWear-routed order until every ☐ under **A** and **B** is ☑.

> **🧊 FIREBASE FEATURE FREEZE — 2026-09-26.** The platform is being fully ported to Cloudflare (`docs/cf-port/PLAN.md`, CP0–1 approved). No new product work lands on Firebase; only `hotfix:` commits that keep melodie-mc's test usable. Remaining A-items (A5/A6/A7/A12) are built on Cloudflare.

---

## A. Platform build (code)

| # | Item | Owner | Status | Notes |
|---|---|---|---|---|
| A1 | **Production-cost withholding** — Σ(itemCostSek × qty × 1.25) from the frozen checkout snapshot folded into `application_fee_amount`; order stamped with the withheld amount; checkout blocks when a routed line has no price or the fee would exceed gross | C | ☑ | **BUILT + Fable-reviewed + committed 2026-09-25** on branch `worktree-agent-ae5381663d337e2e9` (18 suites green, verified independently). Defaults OK'd 2026-09-25 → MERGED to main e69b1e6 + PUSHED e6faeac. NOT deployed (needs explicit go). ⚠️ Known PRE-EXISTING edge (not A1's): base PI metadata can already hit Stripe's 50-key cap when withdrawal+pickup+campaign+affiliate+b2c+Connect all coincide; A1 adds 2 keys on POD Connect orders — track as a separate fix. Plan: `~/.claude/plans/snapwear-production-withholding.md` |
| A1b | Seller-facing order view: **ONE line** "Avgift (plattform & produktion)" + "Utbetalning till butiken" — never the breakdown (Mikael: "One", 2026-09-25) | C | ☑ | **BUILT 2026-09-26** inside A13: `OrderPaymentCard.jsx` — "Avgift (plattform & produktion) −X kr" + "Utbetalning till butiken Y kr", rendered light+dark, Fable-eyeballed. Not on the guest confirmation page. |
| A13 | **"Seller sees ONE number" data-layer hardening** — printers/* platform-only + stripped `printersPublic` projection; cost to the studio via `quotePodCost` callable (client twin loses all tier-cost code); order money (per-line costs, commissionBps, productionWithheldOre) → server-only `orderProduction/{id}`, order keeps `applicationFeeAmount` only; legacy template prices stripped; migration script for pre-existing orders. Reason: `orders/{id}` has `allow get: if true` (guest confirmation) → seller AND buyer could read per-line production costs; `printers/*` was seller-readable by August design. Plan `~/.claude/plans/one-number-money-hardening.md` | C | ☑ | **BUILT 2026-09-26**, Fable-reviewed (rules, orderMoney split, webhook batch.create idempotency, quotePodCost guard, client purge + grep-guard test, re-quote-before-stamp). Audit: `printersPublic`/new orders/`productsPublic` clean; Kim price list REMOVED from the public bundle (was `DEFAULT_TIER`); residual = seller's own `commissionBps` (their contract rate, not a leak) + pre-A13 orders until migration + live templates until reseed. Inference risk accepted: seller knows their % and Inköp → can estimate shipping inside Avgift. |
| A2 | **SnapWear catalog import** — script reads `PrintArea.xlsx` + `SKU 21.09.xlsx` → `printers/{snapwear}` tier (SEK, seeded from EUR at a fixed rate + buffer) + `printerCatalog/snapwear` (per model frames, per SKU colour/size/id). Map by SKU number, never by colour name. Sizes normalized. | C | ☑ | **BUILT 2026-09-25** (branch worktree-agent-a6aee725b681f2a85, Fable-reviewed): `scripts/snapwear-xlsx-to-json.py` → `docs/SnapWearDocs/snapwear-catalog.json` (47 models, 323 SKUs) + `scripts/seed-snapwear-printer.cjs` (DRY-RUN verified; Mikael runs `--eur-sek <rate> --commit --force --route`). Provisional frames: longsleeve/bag/cap/beanie until C1. pricingBasis kept on printerCatalog/* (seller-unreadable). |
| A3 | **Per-printer print areas** — `printers/{uid}.printAreas[garment][slot]` = {w,h,offsetTop} mm; studio + DPI gate use the ROUTED printer's area; slot change re-resolves routing (sleeve → Kim); publish validates against routed printer; reroute shows "no longer fits" warning | C | ☑ | **BUILT 2026-09-25**: printers/{uid}.printAreasMm + applyPrinterAreas (pure, 36 tests) + studio derives one template + PrinterRow 'Tryckytor (mm)' editor rendered 1280/390. Follow-ups: re-validate published products after a frame edit; 3D areas. |
| A4 | **Capability gating before payment** — slots/garments a printer cannot make are HIDDEN in the studio and blocked at publish + checkout (sleeve hidden now; flatcap not in SnapWear catalog → not offerable); one order → one printer (trivially true with a single printer, keep the guard) | C | ☑ | **BUILT 2026-09-25**: default printer must list garment (both twins, parity 66/0); 409 no-printer-for-garment + slot-not-printable; sleeve hidden automatically. |
| A5 | **Canvas PNG per line** — SnapWear frame at print resolution with our motif at our placement offset; stored under the order in the shop's server-owned print path; signed URL | C | ⏸ | Waiting on N: offset reference (collar vs pallet), DPI, max file size |
| A6 | **Outbound submit** — `type:'api'` printer; outbox handler POSTs `/api/order/add` with stable `job_id` (= order id + line), retry-same-id, duplicate-400 = success, 422 = rejected → flag; SKU from catalog map; artwork + mockup URLs | C | ⏸ | Waiting on N: store-vs-fetch artwork, duplicate response text, charge-status in response |
| A7 | Out-of-stock exception status on orders (accepted+charged, then SnapWear emails) | C | ☐ | Small |
| A8 | Shipped status: manual click from SnapWear email (v1) | — | ☑ | Exists (AdminOrderDetail tracking field) |
| A9 | Statement descriptor suffix per shop (`METEORPR* <SHOP>`) | C | ☑ | **Already built** — createPaymentIntent.ts appends the shop's sanitized name as `statement_descriptor_suffix` (verified 2026-09-25) |
| A10 | "Rapportera intrång" link in every shop footer + admin takedown action (unpublish + flag + log) | C | ☑ | **BUILT 2026-09-25** (`89de8af`, merged `dbff373`, Fable-reviewed + rendered): storefront page `/:shopId/rapportera-intrang` (NORD) + footer link; `submitInfringementReport` (anon, honeypot, 5/h per IP, no IP stored) → `infringementReports` (platform-only) + `INFRINGEMENT_REPORT_ADMIN` email; `takedownProduct` (platform) → isActive=false + takedown stamp + auditLogs; platform page **Anmälningar** + nav badge. Rules: seller cannot write screening/takedown nor flip isActive under a takedown. NOT built: payout hold (manual transfer reversal in Stripe if needed). |
| A11 | Pre-publish screening: blocklist (brands/bands/clubs) on name/description/filename + platform review queue for a new shop's first products | C | ☑ | **BUILT 2026-09-25** (same merge): `settings/contentScreening` (seed `scripts/seed-content-screening.cjs`, 63 terms, Mikael runs `--commit`), pure matcher twins (parity 90), `screenProductOnWrite` trigger = sole writer of `screening` (flagged/review/ok/blocked; optional hardBlock → isActive=false), seller sees the notice at publish, **Granskning** tab in Anmälningar. Gaps: ProductForm notice doesn't see artwork names (server does); joined names like "acdc" need their own term. |
| A14 | **CODEX audit 2026-09-26 (`docs/audits/2026-09-26-last-24-hours.md`) — F1–F5 fixed** | C | ☑ | **BUILT 2026-09-26**, all five confirmed real before fixing. F1 seller can no longer delete a taken-down product (rules + AdminProducts guard + 5 rules tests). F2 manual mapping form requires a plagg; garment-less rows get a chip and no longer count toward the live-gate (ProductForm); stale "routes to default printer" comments fixed. F3 PrinterRow keeps an explicit `{}` for an offered garment with no frames (was silently ungated); studio hides a routed garment with zero printable slots; helpers moved to `printerTierForm.js` + test. F4 new triggers `rescreenProductsOnMappingWrite` / `rescreenProductsOnArtworkWrite` re-run screening when artwork names change without a product write. F5 `OrderPaymentCard` payout follows `payment.refundedTotalSek` (`utils/shopPayout.js`, proportional transfer reversal; full refund → 0). NOT fixed (deferred, audit agrees): re-upload under a NEW product id / same SKU after a takedown — needs an artwork-level takedown stamp. Screening stays advisory (`hardBlock:false`) — decide before launch whether first sale needs approval (B11). |
| A12 | *Later:* inbound-email parser (Cloudflare Email Routing + Worker) → auto "shipped" + tracking; fail-safe forwards to a human | C | ☐ | After pilot. Needs N: sample shipping email |

## B. Not code — must exist before first live order

| # | Item | Owner | Status |
|---|---|---|---|
| B1 | Pilot order through the SnapWear portal (Swedish address) | K | ☐ |
| B2 | EUR business account + virtual card for SnapWear only, monthly limit | K | ☐ |
| B3 | Ansvarsförsäkring for Meteor PR AB + quote for IP add-on (standard policies EXCLUDE IP) | K | ☐ |
| B4 | Accountant: reverse-charge VAT on EU purchases + production invoiced to shops with 25% moms (monthly statement per shop) | K/M | ☐ |
| B5 | SnapWear garment certifications (Gildan, Stanley/Stella, AWD…) on file — GPSR distributor duty | K → N | ☐ |
| B6 | DPA (EU SCC 2021/915 incorporated by reference, sub-processing structure, annexes pre-filled) → Natalia's legal team | C → K | ◐ **DRAFTED 2026-09-25**: `docs/SnapWearDocs/DPA_Meteor_PR_AB_Snapwear.md`. Kent fills placeholders, attaches official SCC PDF, sends. Want a .docx? say so |
| B6b | Meteor↔shop biträdesavtal (`docs/legal-template-files/05-personuppgiftsbitradesavtal.md`) must be IN FORCE (accepted via platform terms gate) — the DPA chain is shop→Meteor→Snapwear; without it Meteor has no instructions to act on | M | ☐ |
| B7 | Liability clause in platform terms: remedy = reprint, cap = what SnapWear makes good, no consequential damages; SnapWear's 2-week/3-month window mirrored as far as köplagen allows. Terms-version bump → re-acceptance via existing gate | C → M | ☐ |
| B8 | 24h takedown routine (who watches the inbox, what gets unpublished, when payout is held) written down | K/M | ☐ |
| B10 | **Confirm actual Stripe loss liability before first live order** (Codex 2026-09-25): on destination charges the platform is liable to Stripe for chargebacks, refunds and negative connected-account balances (SE/EU accounts are NOT auto-debited). Read the Connect platform agreement for SE, confirm dispute-recovery code covers reversal + shortfall alerting, decide a reserve policy (e.g. hold X% or delay payouts N days on new shops). Write the answer down. | M/C | ☐ |
| B9a | **DEPLOY ORDER (hard):** prod has NO settings/printRouting today and two ACTIVE Kim tier docs (`o7diaDJ01tRoBMs8d5OK9bPunMg1`, `viTqJwF66NXZ42gEZOxQvqkMBUh1`). Deploying A2–A4 code BEFORE `node scripts/seed-snapwear-printer.cjs --eur-sek <rate> --commit --force --route` = every POD checkout 409 no-printer-for-garment + studio shows zero garments. Sequence: (1) run seed with --route, (2) deactivate Kim tier docs in Tryckerier, (3) deploy. Also: SnapWear-routed lines reach NO ONE until A6 (no print_shop user has uid 'snapwear') — consistent with "nothing live until A+B" but must be understood. | M/C | ☑ **DONE 2026-09-26**: seed --route run by Mikael (EUR/SEK 11.20, buffer 3 %); Kim/Systema + Adapt Media tier docs LEFT ACTIVE (routing points every garment to snapwear anyway — deactivate in Tryckerier when convenient) |
| B9c | **Deploy order for A13 (one-number):** (1) deploy functions (`quotePodCost`, `syncPrintersPublicOnWrite`, webhook), (2) `cd functions && npm run build` → `node scripts/backfill-printers-public.cjs --commit` + the SnapWear seed (`--route`), (3) deploy hosting — publish is REFUSED until `quotePodCost` exists, so functions first, (4) deploy rules, (5) `node scripts/migrate-order-money-to-private.cjs` (dry-run, then --commit), (6) reseed templates `scripts/seed-pod-mockup-templates.cjs --commit --force` (strips legacy Kim prices from the live doc). Open check: whether a connected account can see PI metadata (`commissionBps`, `productionWithheldOre`) in its Stripe dashboard — verify on the pilot. | M/C | ☑ **DONE 2026-09-26**: 81 functions deployed (4 needed a quota retry), printersPublic backfilled (3), 4 melodie-mc orders migrated, hosting ×4 origin-verified `index-EybuBb5L.js`, rules released, templates reseeded |
| B9b | **Deploy order for A10/A11:** (1) `node scripts/seed-content-screening.cjs --commit` (without it only the new-shop review rule applies), (2) deploy firestore rules, (3) deploy functions `submitInfringementReport`, `takedownProduct`, `screenProductOnWrite` (+ the email orchestrator), (4) hosting. First edit of an existing live product after deploy stamps it once. | M/C | ☑ **DONE 2026-09-26**: blocklist seeded (63 terms), rules + fns + hosting live |
| B9d | **Deploy order for the audit fixes (A14):** (1) `node scripts/backfill-pod-mapping-garment.mjs` dry-run → every `unknown-left-null` row is a product checkout will 409 on; sellers re-publish from the studio or re-add the mapping with a plagg (ProductForm now refuses to put those live), (2) deploy firestore rules (F1 delete guard), (3) deploy functions `screenProductOnWrite`, `rescreenProductsOnMappingWrite`, `rescreenProductsOnArtworkWrite` (no index/rules change needed), (4) hosting. Tryckerier: re-save SnapWear once after deploy so every offered garment has an explicit frame entry (F3). | M/C | ☑ **DONE 2026-09-26**: dry-run = 18 garment-less mappings (melodie-mc 15, sillmans 3) → those products 409 at checkout until re-published from the studio; rules/fns/hosting live; SnapWear frames explicit via seed (no re-save needed) |
| B11 | **Decide: advisory screening or approval-before-first-sale?** Seed has `hardBlock:false`; flagged products stay public and checkout-valid (audit: "pre-publish screening" is overstated). If first sale must wait for a human, that needs a pending-review state + checkout gate — not built. | M | ☐ |
| B9 | Runbook: test API with auto-pay OFF → cancel every test order → enable auto-pay → first live order. Daily dashboard glance for unpaid orders until charge-status question answered | C | ☐ |

## C. Questions still open with Natalia

| # | Question | Blocks |
|---|---|---|
| C1 | Print frames for Gildan 64400, SF500, Westford Mill W101, Beechfield B445 patch, trucker cap panel, posters | A2, A5 |
| C2 | "Offset from top" — measured from collar seam or pallet edge? | A5 |
| C3 | Artwork file: DPI, max file size, transparent PNG accepted? | A5 |
| C4 | Do you store the artwork at import, or fetch from our URL at print time (reprints)? | A6 |
| C5 | Exact response for duplicate `job_id`; meaning of `design` field; neck-label payload key | A6 |
| C6 | With auto-pay on, does the API response show charge success? What happens on a failed charge? | B9 |
| C7 | Sample shipping-notification email — does it carry our job_id + tracking number? | A12 |
| C8 | Shipping rate to Sweden per weight band (for the tier's shipping basis) | A1 tier |
| C9 | Agent/reseller-of-record model? (ask once, expect no) | — |

## D. Decisions Mikael has taken / still owes

| Decision | Status |
|---|---|
| SnapWear = primary printer; Kim/SE = sleeve-only secondary | ☑ 2026-09-23 |
| Per-printer print areas | ☑ 2026-09-23 |
| Current one-way status flow accepted; email automation later | ☑ 2026-09-23 |
| Reseller model accepted with hardening (A9–A11, B3, B7) | ☑ 2026-09-25 |
| Withhold production INKL 25% moms (platform sells production to shop) | ☑ ok 2026-09-25 |
| POD checkout requires Stripe Connect on the shop (no Connect → cannot withhold → block) | ☑ ok 2026-09-25 — ⚠️ still need the list of chargesEnabled shops before deploy (`node scripts/report-legal-readiness.cjs`) |
| `refundApplicationFee` policy once POD is live | ☑ ok 2026-09-25 → set `settings/platform.refundApplicationFee=false` at go-live (settings write, no code) |
| SnapWear shipping: flat per-order SEK in `printers/{uid}.shippingSek`, withheld once per printer per order | ☑ ok 2026-09-25 (value pending C8) |
| Kim's fate | ☑ 2026-09-25 **REMOVED** — SnapWear is the ONLY printer; reinstate if ever needed. Consequence: sleeve slot HIDDEN in studio until a printer supports it (A3/A4 collapse to single-printer + capability gating) |
| DPA draft | ☑ GO 2026-09-25 → `docs/SnapWearDocs/DPA_Meteor_PR_AB_Snapwear.md` |
