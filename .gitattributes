# Wrangler generates a few type-only lines with trailing spaces. Keep the
# generated file byte-identical so `wrangler types --check` remains reliable.
cloudflare/worker-configuration.d.ts -whitespace
